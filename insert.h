
#ifndef INSERT_H
#define INSERT_H

#include "parseTree.h"

void value(node*);
void valueList(node*);
void insertTuples(node*);
void attrName(node*);
void attrList(node*);
void insertStmt(node*);

#endif
